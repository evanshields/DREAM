[pytest]
# DREAM backend test config. Run from the repo root:
#   .venv/Scripts/python -m pytest underwriting-engine/engine/tests underwriting-engine/fastpath/tests backend/tests -q
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
