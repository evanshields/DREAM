# DREAM — Handoff to next chat (2026-07-12, post 24h-sprint)

Paste-prompt at the bottom. Read this fully before doing anything — the app is LIVE and current;
do not redo shipped work.

## STATE (verified live, end of sprint)

- **ONE repo for everything DREAM (consolidated 2026-07-12):** `github.com/evanshields/DREAM`,
  local `c:\Users\evana\DREAM`, branch **`wave-a-foundation`** — app code (`backend/`,
  `frontend/`, `underwriting-engine/`) AND all product docs (**`docs/`** — PRDs, handoffs,
  sprint log, this memo). The old doc home (`shieldstone_os\dream\`) is retired to a pointer;
  the dream-docs commit on the Shieldstone monorepo's built-different branch was removed
  (history rewritten) — nothing DREAM lives in the Shieldstone repo anymore.
  **PR #3 (branch → main) still needs Evan's one-click merge** (sandbox blocks agent self-merge).
- **LIVE:** https://dream.shieldstone.co — US VPS 72.61.5.208 (SSH port 2222, key
  `~/.ssh/id_ed25519`), PM2 `dream-api` on :8001, Caddy serves `/opt/dream-app/frontend`
  (SPA fallback) + proxies `/api/*`. **The server is NOT a git checkout** — its connection to
  the repo is the deploy pipeline: build/scp from the local `c:\Users\evana\DREAM` clone +
  `pm2 restart dream-api --update-env`. Live code == repo @ the last deployed commit.
  Timestamped `.bak-*` backups of backend + frontend on the VPS from every deploy.
- **Tests: 356 passed, 1 skipped** (the live-Kimi skip). Run from repo root:
  `.venv/Scripts/python -m pytest underwriting-engine/engine/tests underwriting-engine/fastpath/tests backend/tests -q`
  Frontend: `cd frontend && npx tsc --noEmit && npm run build` (only warning = recharts chunk size).
- **Accounts:** `evan` (password in Evan's keeping) · `charles` / `<password redacted - credential with Evan/Charles>` ·
  Google sign-in configured (Test Users added). Auth = app JWT OR Google token, issuer-dispatched.
- **Kimi live** on server (`DREAM_USE_KIMI=1`, moonshot-v1-128k, parallel slices).

## WHAT SHIPPED IN THE 2026-07-11/12 SPRINT (all live-verified)

Full log: `docs/DREAM_V3_BUILDOUT_PLAN_2026-07-11.md`. Summary:
1. **Bond Screen** (`/bond-screen`) — deterministic EFB sizing + exit-cap triangulation + agency
   sizing calculators.
2. **EFB route unlocked end-to-end** — wave0 accepts explicit routing=EFB (critical input:
   stabilized_noi; per-routing gating), synthesis dispatches ACQ|EFB via `run_efb_underwrite`,
   qa_gates EFB-aware (formula_audit EXPLICIT skip w/ reason). Live-verified to CP-1 with exact
   Rayzor truth ($63,868,907 bonds / 1.15x / $9M tax savings). ACQ path byte-identical (Esplanade).
3. **EFB frontend** — ACQ/EFB intake selector, bond-metric CP-1 tiles + seeded EFB sizing panel,
   EFB pipeline cards, documented-skip gate badges.
4. **GET /api/deals/{deal_id}** — full deal view (spec + gates + latest job) — cold-reload fixed.
5. **PDF intake** — drag-drop OM/T-12/rent-roll on Underwrite → live extraction prefills fields
   (routing-aware, tagged, editable) → extracted text rides as `deal_docs.uploaded_doc` (50K cap).
6. **Memo** — `POST /api/deals/{id}/memo` (routers/memo.py, LLM, forced GENERATED DRAFT header,
   persisted `spec.narrative.memo`) + Memo tab (generate/regenerate/download, markdown renderer).
7. **Audit trail** — `GET /api/deals/{id}/audit` + Activity tab (kind-coded timeline).
8. **Excel export UI** — Export tab (Mini Model .xlsx template upload → populate → download;
   BL refusals rendered).
9. **Bugs fixed during sprint:** deal names wiped at CP-1 persist (runner meta carry-forward);
   array answers leaked to LLM slices (BL-17-trio-only to slices; non-scalar echo cells dropped).
10. **Security:** `/api/intake`, `/api/agent/chat`, `/api/agent/memo` were ALL ungated Kimi doors —
    closed (verified 401 bare).

## LOCKED CONTRACTS (violate = failure)

Decimal only in `engine_boundary.py` + vendored engine · spec is canonical · HITL stops at
AWAITING_CP1 · no sqlite3 outside `backend/store/` · headline_metrics + qa.* NEVER from LLM ·
fail closed on RED gates · vendored `underwriting-engine/` untouched · LLM slices receive ONLY
wave0's scalar critical inputs (never arrays) · Esplanade (ACQ) + Rayzor (EFB) are the oracles.

## NEXT UP

**Phase 4 (approved to consider, not started):**
- **4a Async submit** — biggest UX/robustness item: enqueue + poll instead of the synchronous
  multi-minute POST (runner.py is already the worker body per its docstring; router swaps
  run_job call for enqueue; frontend polls GET /api/jobs/{id}). Watch: proxy timeouts today.
- **4b** archive/delete deals, owner scoping, AssumptionDashboard loads ANY deal's spec (currently
  Esplanade defaults only on ACQ deals).
- **4c misc:** Google-cert caching in verify_google_token; 'hap'/'noah' substring false-EFB-flag in
  wave0; legacy `/api/underwrite` still on old unvalidated engine (drift risk — retire or mark).

**Claude Design pass (Evan driving, next chat supports):** Evan takes the frontend into
claude.ai/design (link GitHub repo `evanshields/DREAM` → `frontend/` folder as design-system
source + live-app screenshots), restyles pages, exports via "Handoff to Claude Code" or zip.
Next chat integrates returned code into `frontend/`, verifies tsc/build/API wiring intact,
tests, deploys. The `DesignSync` tool exists in-session for pushing a curated design-system
bundle to his Claude Design org if wanted (the /design-sync skill is not installed; hand-rolled
bundle = heavier lift, optional).

**Standing open items:** Evan merges PR #3 · rotate Kimi key + Google Client Secret (rode through
chats) · evan password → password manager · Postgres/Wave F designs exist
(`backend/store/WAVE_F_POSTGRES_DESIGN.md`, `backend/WAVE_F_FULL_AUTH_DESIGN.md`) — deferred.

## WORKING PATTERNS THAT HELD ALL SPRINT

- Subagent builders draft in working tree (no commit/deploy) → main session reviews diff → full
  suite + tsc/build → commit → push → backup on VPS → scp changed files → restart → **live-verify
  against production** (curl with a minted token; mint via SSH:
  `cd /opt/dream-app/backend && export AUTH_JWT_SECRET=$(grep ^AUTH_JWT_SECRET= .env | cut -d= -f2-) && venv/bin/python -c "import app_jwt; print(app_jwt.mint_token(\"evan\",\"evan@shieldstone.co\"))"`
  — or just log in as charles via the API).
- Every live verification caught something a test didn't at least once — do not skip it.
- Caveman mode was active at session end (Evan invoked /caveman) — next chat starts fresh unless
  he re-invokes.

## PASTE-PROMPT FOR NEXT CHAT

> Read `c:\Users\evana\DREAM\docs\HANDOFF_2026-07-12.md` fully — the app is live and
> current; do not redo shipped work. Continue the DREAM v3.0 buildout: Phase 4 per §NEXT UP
> (async submit first), honoring every LOCKED CONTRACT and the working patterns (review → test →
> deploy → live-verify). If Evan brings back Claude Design code, integrate it into frontend/
> without breaking the API wiring, test, deploy. Keep the buildout plan log updated
> (`docs/DREAM_V3_BUILDOUT_PLAN_2026-07-11.md`).
